pipeline {
    agent {
      label 'lms-project-fe'
    }

    environment {

    DOCKERHUB_CREDENTIALS = credentials('yoursureshdockerhub')
     registry = "yoursureshdockerhub/webapp"
        registryCredential = 'yoursureshdockerhub'
        dockerImage = ''
    }

    stages {
        

        stage('Building the docker image') {
            steps {
                sh 'cd webapp && docker build -t yoursureshdockerhub/webapp .'
            }
        }
        stage('Logging into dockerhub account') {
            steps {
                sh 'echo $DOCKERHUB_CREDENTIALS_PSW | docker login -u $DOCKERHUB_CREDENTIALS_USR --password-stdin'
            }
        }
        stage('pushing the docker image into dockerhub') {
            steps {
                  sh 'docker push yoursureshdockerhub/webapp'
            }
        }
        stage('Remove old docker images') {
             steps {
                 sh 'docker rmi -f yoursureshdockerhub/webapp'
            }
        }
         stage('Running the docker container') {
            steps {
                  sh 'docker container rm --force f-end'
                  sh 'docker run -dt -p 8000:80 --name f-end yoursureshdockerhub/webapp'
            }
        }
    }
}

